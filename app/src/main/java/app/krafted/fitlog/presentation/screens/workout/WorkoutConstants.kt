package app.krafted.fitlog.presentation.screens.workout

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp

object WorkoutConstants {
    // Colors
    val PR_GOLD = Color(0xFFFFD700)
    val PR_GOLD_DARK = Color(0xFFB8860B)
    
    // Layout
    val TOUCH_TARGET_MIN = 48.dp
    val INPUT_ROW_PADDING_VERTICAL = 4.dp
    val INPUT_ROW_PADDING_HORIZONTAL = 4.dp
    
    // Timers (Seconds)
    const val REST_TIMER_DEFAULT_SECONDS = 90
    
    // Limits
    const val RECENT_SETS_HISTORY_LIMIT = 5
}
